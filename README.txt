Para que funcione la base de datos es necesario tener instalado el node 16
una vez instalado ejecutar en la consola

npm install -g json-server
npx json-server --watch db.json

esperar y ya estaria funcionando.

